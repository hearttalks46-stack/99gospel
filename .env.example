# Inbox that receives client form submissions
EMAIL_TO=support@cryptosolutionagency.com
EMAIL_FROM=Crypto Solution Agency <support@cryptosolutionagency.com>

# Hostinger mailbox SMTP (hPanel → Emails)
SMTP_HOST=smtp.hostinger.com
SMTP_PORT=465
SMTP_USER=support@cryptosolutionagency.com
# Required. Mailbox password, not the Hostinger account password unless they are the same.
SMTP_PASS=your_mailbox_password_here
